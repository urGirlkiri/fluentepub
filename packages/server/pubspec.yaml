name: server
description: backend for fluent epub
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.11.0
resolution: workspace

dependencies:
  dart_frog: ^1.1.0

dev_dependencies:
  dart_frog_lint: ^0.1.0
  mocktail: ^1.0.0
  test: ^1.0.0
